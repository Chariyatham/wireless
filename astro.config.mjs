import { defineConfig } from 'astro/config';

// repo: Chariyatham/wireless  →  https://chariyatham.github.io/wireless/
export default defineConfig({
  site: 'https://chariyatham.github.io',
  base: '/wireless',
});
