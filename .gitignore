# ===== repo public — อาจารย์อนุญาตให้เผยแพร่สไลด์/เสียง/transcript ได้แล้ว =====
# (เสียง/สไลด์/transcript จึง "ไม่" ถูก ignore แล้ว — ขึ้น repo ได้)

# Python venv (ก้อนใหญ่ + regenerate ได้ ไม่ควรขึ้น git)
tools/whisper-venv/
__pycache__/
*.pyc

# graphify ผลลัพธ์กลาง (build artifact — ตัวคอนเซปต์แมปสุดท้ายจะ copy ไป site/ ต่างหาก)
graphify-out/

# tooling / os
.DS_Store
.claude/
.agents/
skills-lock.json

# Node / Astro
node_modules/
dist/
.astro/

tools/_audio_tmp/
